{{#if this.isPagePerson}}
<b>{{@person.name}}</b>
{{else if @referencePerson}}
<LinkTo @route="person" @model={{@person}} @query={{hash referencePersonId=@referencePerson.id}}>
    {{@person.name}}
</LinkTo>
{{else}}
<LinkTo @route="person" @model={{@person}}>
    {{@person.name}}
</LinkTo>
{{/if}}