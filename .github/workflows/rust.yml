name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  # This workflow is now integrated into .github/workflows/ci.yml
  # Keeping this file for backwards compatibility but the actual tests
  # are run in the unified CI workflow
  placeholder:
    runs-on: ubuntu-latest
    steps:
    - name: Tests moved to CI workflow
      run: echo "Rust tests have been moved to .github/workflows/ci.yml"
